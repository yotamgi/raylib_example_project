# raylib_example_project

Example raylib example project, with both linux and web platforms.

To compile and run the linux platform, just do

```
make run_linux
```

To compile and run for web platform, just do
```
make run_web
```
